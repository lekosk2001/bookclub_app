import { useRouter } from 'next/router';
import React from 'react'
import BoardDetail_UI from './BoardDetail_UI';

type Props = {}

const BoardDetail_container = (props: Props) => {

    return (
        <BoardDetail_UI />
    )
}

export default BoardDetail_container